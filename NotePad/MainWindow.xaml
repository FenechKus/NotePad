<Window x:Class="NotePad.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NotePad"
        xmlns:viewmodel="clr-namespace:NotePad.MVVM.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <viewmodel:MainViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel>
            <Button Content="SortByTitle" Command="{Binding SortyngByTitleCommand}"/>
            <Button Content="SortByTitleDec" Command="{Binding SortyngByTitleDecCommand}"/>
            <Button Content="Add" Command="{Binding AddCommand}"/>
            <Button Content="Remove" Command="{Binding RemoveCommand}"/>
            <ListBox ItemsSource="{Binding Notes}" 
                     SelectedItem="{Binding SelectNote}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding Title}"/>
                            <TextBlock Text="{Binding DateCreate}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
        <StackPanel Grid.Row="1" DataContext="{Binding SelectNote}">
            <TextBox Height="100" Text="{Binding Title}"/>
            <TextBox Height="100" Text="{Binding Description}" TextWrapping="WrapWithOverflow"/>
        </StackPanel>
    </Grid>
</Window>
